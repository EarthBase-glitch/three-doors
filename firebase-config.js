// Fill this in with the values from:
// Firebase console -> Project settings (gear icon) -> Your apps -> SDK setup and configuration
// This file is safe to be public — it identifies your project, it does not
// grant access on its own (that's what firestore.rules controls).
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
